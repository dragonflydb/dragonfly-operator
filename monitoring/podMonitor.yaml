apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: dragonfly-monitor
  labels:
    group: dragonfly
    app: dragonfly-monitor
spec:
  selector:
    matchLabels:
      app: dragonfly-sample
  podMetricsEndpoints:
  - port: admin
